// Test: Edge Cases
// EXPECT: 27

#main() >
    // Zero operations
    x := 0.
    y := 0 + 0.
    z := 0 * 100.
    
    // Negative numbers
    a := 0 - 1.
    b := 0 - 100.
    c := a * b.  // 100
    
    // Large numbers
    big := 1000000.
    result := big / 1000.  // 1000
    
    // Empty loop (should not execute body)
    count := 0.
    for i in 0..0 >
        count = 999.
    <
    
    // Deeply nested
    sum := 0.
    for i in 0..3 >
        for j in 0..3 >
            for k in 0..3 >
                sum = sum + 1.
            <
        <
    <
    // 3 * 3 * 3 = 27
    
    /console_log_int/sum.
    << 0.
<
